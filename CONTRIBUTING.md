# 贡献指南

感谢你考虑为 Recall 做贡献。Recall 处理用户工作上下文，因此我们将**隐私、安全、可解释性与可删除性**视为功能正确性的一部分。任何贡献都应保持“明确触发、本地优先、来源可追溯”的项目原则。

## 开发环境

推荐使用 macOS 14+、最新版 Xcode 和 Swift 6。克隆仓库后，先验证核心逻辑：

```bash
swift build --jobs 1
swift run RecallVerifier
```

`RecallVerifier` 不使用真实截图、真实 API Key 或网络请求，可在 CI 与本地安全执行。完整 Xcode 可用于运行 `RecallApp`、检查屏幕权限、调试菜单栏交互和本地通知。

## 提交流程

请为每个逻辑变更创建聚焦的分支和 pull request。PR 描述应说明用户可见行为、涉及的数据类别、是否引入新权限、数据是否可能离开设备，以及验证命令与结果。若更改事件模板、截图策略、云端上下文、数据保留或提醒行为，必须同步更新 `README.md` 和 `docs/PRIVACY.md`。

| 变更类型 | 必要说明 |
|---|---|
| 采集能力 | 触发条件、最小采集范围、权限、暂停与撤销路径。 |
| OCR 或索引 | 是否在本地运行、失败路径、保存内容与删除策略。 |
| 云端模型 | 外发字段、脱敏策略、默认开关、密钥保存位置与失败回退。 |
| 提醒或自动化 | 用户确认点、频率限制、来源展示与关闭路径。 |
| UI 改动 | 截图或录屏、无障碍影响和手动验证步骤。 |

## 代码约定

核心逻辑位于 `Sources/RecallKit`，并尽量保持框架边界清晰：采集、隐私、存储、检索、会话和提醒不应互相隐式访问 UI 状态。SwiftUI 界面位于 `Sources/RecallApp`。不要将 API Key、个人截图、OCR 数据、会话导出或真实 Bundle ID 样例提交到仓库。

新增功能应优先提供可在无图形界面下验证的纯逻辑或验证器检查。涉及系统权限的部分应把实际 OS 调用抽象在窄接口后面，便于模拟和审查。

## Issue 规范

功能请求请说明要解决的问题，不要只给出实现方案。Bug 报告应说明 macOS 版本、Recall 版本、复现步骤和脱敏后的日志；请不要贴出敏感截图、私密文本、云端密钥或可识别身份的信息。安全和隐私漏洞请不要公开提交，参见 [SECURITY.md](SECURITY.md)。

## 行为准则

请保持建设性、尊重和包容。我们不接受骚扰、歧视、公开泄露他人数据或试图绕过隐私保护的贡献。
